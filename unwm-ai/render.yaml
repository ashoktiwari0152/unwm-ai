services:
  - type: web
    name: unwm-ai
    env: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      python -m uvicorn backend:app --host 0.0.0.0 --port 10000